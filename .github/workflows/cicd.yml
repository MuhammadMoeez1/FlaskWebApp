name: devops app

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

permissions:
  id-token: write
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest
    # run all `run:` steps inside the FlaskWebProject1 folder where the app lives
    defaults:
      run:
        working-directory: FlaskWebProject1
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: List Files
        run: echo "Moeez is not great"

     
